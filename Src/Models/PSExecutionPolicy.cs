namespace Hangfire.PowerShellExecutor
{
    public enum PSExecutionPolicy
    {
        AllSigned,
        Bypass,
        Default,
        RemoteSigned,
        Restricted,
        Undefined,
        Unrestricted
    }
}