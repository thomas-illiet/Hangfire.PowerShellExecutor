namespace Hangfire.PowerShellExecutor
{
    public enum ExecutionPolicy
    {
        AllSigned,
        Bypass,
        Default,
        RemoteSigned,
        Restricted,
        Undefined,
        Unrestricted
    }
}